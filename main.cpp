



#include <iostream>
#include "List.h"


using namespace std;


int main(){

    // Node *head =nullptr;
    // append(&head, 5);
    // append(&head, 10);
    // push(&head, 2);
    // pushAfter(head->next, 4);
    // printList(head);
    
    return 0;
}